---
import "../styles/event.css";
import BaseLayout from "../layouts/baseLayout.astro";

import { getCollection, type CollectionEntry } from "astro:content";
import UpcomingEvents from '../components/upcomingEvents'; 

const now = new Date();
now.setHours(0, 0, 0, 0);

const publishedEvents: CollectionEntry<'events'>[] = await getCollection('events', ({ data }: CollectionEntry<'events'>) => {
  return data.status === 'published';
});

const townsByCountry: Record<string, Set<string>> = {};
const rinks = new Set<string>();
const ages = new Set<string>();

// 3. Single-Pass: Filter, Sort, Serialize, and Scan
const clientUpcomingEvents = publishedEvents
  .filter((event: CollectionEntry<'events'>) => event.data.startDate >= now)
  .sort((a, b) => a.data.startDate.valueOf() - b.data.startDate.valueOf())
  .map((event) => {
    const d = event.data;
	console.log(`[Astro Server] Processing: ${event.id}`);
    console.log(`[Astro Server] Raw Poster Object:`, d.eventPoster);

    // --- NORMALIZATION HELPER ---
    const clean = (str: string) => str ? str.trim().replace(/\s+/g, ' ') : "";
    const city = clean(d.townCity);
    const rink = clean(d.rink || ""); 

    // --- SCANNING (Building our filter options) ---
    if (d.country && city) {
      if (!townsByCountry[d.country]) townsByCountry[d.country] = new Set();
      townsByCountry[d.country].add(city);
    }
    if (rink) rinks.add(rink);
    if (d.minAge) ages.add(d.minAge);

    // --- SERIALIZATION (The data React receives) ---
    return {
      id: event.id,
	  slug: event.id, 
      data: {
        ...d,
        townCity: city,
        rink: rink,
        eventPoster: d.eventPoster && typeof d.eventPoster === 'object' 
          ? d.eventPoster.src 
          : d.eventPoster,
        startDate: d.startDate.toISOString(),
        endDate: d.endDate ? d.endDate.toISOString() : null,
      },
    };
  });

// 4. Finalize the Options Object
const serverOptions = {
  townsByCountry: Object.fromEntries(
    Object.entries(townsByCountry).map(([country, citySet]) => [
      country, 
      Array.from(citySet as Set<string>).sort()
    ])
  ),
  rink: Array.from(rinks).sort(), // React handle the "All"
  minAge: Array.from(ages).sort((a, b) => parseInt(a) - parseInt(b))
};



---

<!-- TODO: tidy page start, add CSS  -->
<BaseLayout>
<section class="intro">
	<div class="primary-container">
		<!-- title container -->
		<div> 
			<h1>Events</h1>
		</div>
	</div>
</section>
<section class="scribe">
	<div class="primary-container scribe-content">
		<div class="">
			<p>Find Roller skate events happening all over Europe. Small jams, weekend festivals, workshops and more.</p>
	</div>
	<div class="left-lean">
		<p>what are you looking for?</p>
	</div>
	<div class=" half-width-block jam-link-block">
		<h3 id="jam-list-heading">Big Jam List</h3>
		<a href="/festivals" class="button primary-button">Annual Skate Festivals</a>
		<span>Find all of Europes multi-day skate festivals</span>
	</div>
	</div>
</section>
<!-- ||-----------------------------------------------EVENTS SECTION -->
<section class="events-list">
	<!-- ||------------------------------------------EVENTS TEST -->
	<div class="primary-container">
		
		<div>
			<span>Roller skate events</span>
			<h2>Upcoming Events</h2>
		</div>

		<UpcomingEvents 
			client:load
			initialEvents={clientUpcomingEvents}
			serverOptions={serverOptions}
		/>

	</div>

</section>
<!-- ||------------------------------------------------50/50 SECTION -->

<section class="fifty-fifty-section">
	<!-- TODO: create button components / correct stacking order on small screens -->
	<!-- 50/50 submit an  -->
	<div class="primary-container halfbox">
		<div class="image">
			<img src="/src/assets/test1.jpg" alt="" id="half-img">
			
		</div>
		<div class="content-wrap">
			<!-- <div class="pattern">
				<img src="#" alt="#">
			</div> -->
			<div class="inner-wrap">
				<h2>Submit an Event</h2>
				<p>Have an Event, Competition, Jam or Tour to tell us about? We aim to add events weekly, so use the button below to spread the word!</p>
				<div class="btn-wrap">
					<a class="button primary-button" href="/submit-an-event" target="_self">SUBMIT</a>                        
				</div>
			</div>
		</div>
	</div>
</section>

<section class="past-events">
	<!-- past events -->
	<div class="primary-container">
		<div>
			<h3>Last Month's Events</h3>
		</div>
		<!-- {pastEvents.length > 0 ? (
		<div class="event-grid"> 
			{pastEvents.map((post )  =>
				( <EventCard 
				/>)
			)}
		</div>) : 
		(<div class="none-returned"><p>no events found</p></div>)
		} -->

	</div>
</section>
</BaseLayout>

<style is:global>

	.none-returned {
		display: flex;
		justify-content: center;
		justify-self: center;
		align-items: center;
		border-radius: 8px;
		border: 12px double var(--mid-teal);
		background-color: var(--mid-teal);
		width: 70%;
	}
</style> 